package com.example.myapplication.Class;

import java.util.ArrayList;

public class AddressInfo {
    private String pic;
    private String description;
    public int id;
    private static String image_pic[]={

    };
    private static String address_info[]={

    };
   /* public static ArrayList<ShopMaid> getDefaultList() {
        ArrayList<AddressInfo> list = new ArrayList<>();
        for (int i = 0; i < mName.length; i++) {
            ShopMaid info = new ShopMaid();
            info.id = i;
            info.name = mName[i];
            info.pic = mPic[i];
            info.price = mPrice[i];
            info.description = mDisc[i];
            list.add(info);
        }
        return list;
    }*/
}
